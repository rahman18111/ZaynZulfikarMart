# Masukkan URL endpoint backend API jika frontend di-deploy terpisah (misal di Vercel/Netlify)
# Contoh: VITE_API_URL=https://zaynzulfikar-api.onrender.com/api
# Jika dibiarkan kosong, sistem otomatis memakai fallback relative /api atau localhost:5000 saat development.
VITE_API_URL=
